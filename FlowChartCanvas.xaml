<Page
    x:Class="SBPMSearchTest03.FlowChartCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SBPMSearchTest03"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          x:Name="flowchartCanvas"
          SizeChanged="flowchartCanvas_SizeChanged">
        <Grid.Resources>
            <Style x:Name="gridRectStyle" TargetType="Rectangle" x:Key="gridRectStyle">
                <Setter Property="StrokeThickness" Value="1"/>
                <Setter Property="Stroke" Value="Gray"/>
                <Setter Property="Fill" Value="Transparent"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
    </Grid>
</Page>
